Create docker-compose file with .yaml extension

docker-compose up (fow windows)
sudo ./docker-compose up  (for linux)

Once Contianers are running switch to another terminal and run
docker ps -a

browse the localhost. 

to stop Ctrl+C or 
docker-compose down